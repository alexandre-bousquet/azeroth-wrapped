-- Stable UI namespace and transient presentation state.
local _, AW = ...

AW.UI = {
    periodKey = "WEEK",
    previewMode = false,
    detailKey = nil,
    npcSearchText = "",
    companionSearchText = "",
    characterSearchText = "",
    activityFilter = "all",
    characterFilter = nil,
    cards = {},
    periodButtons = {},
}

